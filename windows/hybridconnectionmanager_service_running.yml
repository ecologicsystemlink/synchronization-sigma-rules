- dataTypes:
  - wineventlog
  name: HybridConnectionManager Service Running
  impact:
    confidentiality: 3
    integrity: 3
    availability: 3
  category: Persistence
  technique: T1554
  adversary: UNKNOWN
  description: Rule to detect the Hybrid Connection Manager service running on an
    endpoint.
  references:
  - https://twitter.com/Cyb3rWard0g/status/1381642789369286662
  where: (safe("log.eventCode", 0.0) == double(40300) || safe("log.eventCode", 0.0)
    == double(40301) || safe("log.eventCode", 0.0) == double(40302)) && (""safe("log.hybridConnection",
    "")"" || ""safe("log.sb", "")://"" || ""safe("log.servicebusWindowsNet", "")""
    || ""safe("log.hybridConnectionManage", "")"")
